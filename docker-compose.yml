services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: mentorix-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mentorix}
      POSTGRES_USER: ${POSTGRES_USER:-mentorix}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mentorix}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mentorix -d mentorix"]
      interval: 5m
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: mentorix-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5m
      timeout: 5s
      retries: 10

  mongo:
    image: mongo:7
    container_name: mentorix-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 5m
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./API
      dockerfile: Dockerfile
    container_name: mentorix-api
    restart: unless-stopped
    env_file:
      - ./CONFIG/local.env
    ports:
      - "8000:8000"
    volumes:
      - .:/workspace
    environment:
      APP_ENV: docker
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      GROUNDING_WORKSPACE_ROOT: /workspace
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mongo:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 5m
      timeout: 5s
      retries: 10

  frontend:
    image: nginx:alpine
    container_name: mentorix-frontend
    restart: unless-stopped
    ports:
      - "5500:80"
    volumes:
      - ./frontend:/usr/share/nginx/html:ro
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://localhost/ || exit 1"]
      interval: 5m
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  redis_data:
  mongo_data: